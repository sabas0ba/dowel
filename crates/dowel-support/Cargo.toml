[package]
name = "dowel-support"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Spans, source maps, diagnostics, structured logging, and JSON output. Depends only on the standard library."

[dependencies]
