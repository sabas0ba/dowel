[package]
name    = "app"
version = "0.1.0"

[features]
# `fast` は `simd` を連鎖して有効にする。既定は `fast` のみ。
default = ["fast"]
fast    = ["simd"]
simd    = []
trace   = []
json    = []

[[dependencies]]
name     = "json"
path     = "../json"
optional = true
